
@model IEnumerable<NereyiGezelim.Models.kategori>

@{ 
    Layout = null;
}
<h4>Kategoriler</h4>
<div class="row">
    <div class="col-lg-6">
        @foreach (var item in Model)
        {
            <ul class="list-unstyled">
                <li>
                    <a href="/Home/KategoriYer/@item.kategoriid ">@item.kategoriadi</a> (@item.yers.Count())
                </li>

            </ul>

        }

    </div>
</div>
@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.kategoriadi)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.kategoriadi)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.kategoriid }) |
                @Html.ActionLink("Details", "Details", new { id=item.kategoriid }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.kategoriid })
            </td>
        </tr>
    }

    </table>*@